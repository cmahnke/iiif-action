name: Publish Docker image
on:
  push:
    branches:
      - main

jobs:
#  tag-snapshot:
#    name: Add a tag to satisfy stupid action runner
#    runs-on: ubuntu-latest
#    steps:
#    - name: Tag commit
#      uses: tvdias/github-tagger@v0.0.1
#      with:
#        repo-token: "${{ secrets.GITHUB_TOKEN }}"
#        tag: "latest"


  push_to_registry:
    name: Push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0
      - name: Login to GitHub Packages Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Push to GitHub Packages
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: cmahnke/iiif-action:latest

  test:
    name: Test the build GitHub action
    needs: push_to_registry
    runs-on: ubuntu-latest
    steps:
      - name: Login to GitHub Packages Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull iamge
        run: docker pull cmahnke/iiif-action/iiif-action:main
#      - name: Login to GitHub Package Registry
#        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u "${{ github.actor }}" --password-stdin
      - name: Create IIIF derivates
#          container:
#              image: cmahnke/iiif-action/iiif-action:main
#              credentials:
#                 username: ${{ github.actor }}
#                 password: ${{ secrets.GITHUB_TOKEN }}
        uses: docker://docker.pkg.github.com/cmahnke/iiif-action/iiif-action:main
#        uses: cmahnke/iiif-action@latest
        with:
          args: 'vips dzsave test/2010_02-March-April_Whaling_01.jpg test/  -t 512 --layout iiif --id ''.'''
      - name: List results
        run: |
          ls test
